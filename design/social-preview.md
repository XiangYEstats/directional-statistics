# Social preview

- Source: `static/og.png` (1731 × 909 pixels).
- Build output: `docs/assets/og.png`.
- Used by the home page's Open Graph and Twitter image tags in
  `templates/base.html` when `SITE["url"]` is set in `site_data.py`.

Replace the source image and rebuild to update the preview. The polar graphic is
decorative and does not represent research data.
